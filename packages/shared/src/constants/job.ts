export enum JobSalaries {
  yearly = 'yearly',
  hourly = 'hourly',
}

export enum TypeOfWork {
  onsite = 'onsite',
  remote = 'remote',
  hybrid = 'hybrid',
  inPerson = 'inPerson',
}

export enum WorkingTime {
  fullTime = 'fullTime',
  partTime = 'partTime',
  contractor = 'contractor',
  seasonal = 'seasonal',
}

export enum Experience {
  low = 'low',
  entry = 'entry',
  mid = 'mid',
  high = 'high',
  professional = 'professional',
}

export enum JobApplicationStatus {
  applied = 'applied',
  interview = 'interview',
  hired = 'hired',
  rejected = 'rejected',
  expired = 'expired',
  notAFit = 'notAFit',
}

export enum JobStatus {
  active = 'active',
  paused = 'paused',
}
