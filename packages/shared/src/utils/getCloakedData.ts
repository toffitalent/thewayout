export const getCloakedData = (value: string): string => `${value[0]}****`;

export const getFullyCloakedData = (): string => '*****';

export const getCloakedEmail = (value: string): string => `${value[0]}****@*****.***`;
